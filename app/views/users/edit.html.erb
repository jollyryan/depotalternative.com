<h2>Edit <%= @user.email %></h2>

<%= form_for(@user) do |f| %>

  <p><%= f.label :email %><br />
  <%= f.text_field :email %></p>

  <%= f.collection_select :role, User::ROLES, :to_s, :humanize %>

  <p><%= f.submit "Update" %></p>
<% end %>

<%= link_to "Back", :back %>