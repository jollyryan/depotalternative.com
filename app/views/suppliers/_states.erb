<% if @states %>
  <%= collection_select @filter, :state_id, @states, :id, :name, {:id => 'state_search', :selected => params[:state_id], :prompt => '--Select a State--'}, {:onchange => "$('#suppliers_search').submit()"}%>
<% end %>

